apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-spitfire-spark-ui
  labels:
    app: {{ template "spitfire.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: ClusterIP
  ports:
  - port: 4040
    name: spitfire-spark-ui
  selector:
    app: {{ template "spitfire.name" . }}
    release: {{ .Release.Name }}
