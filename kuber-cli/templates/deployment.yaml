apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-kuber-cli
  labels:
    app: {{ template "kuber-cli.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  strategy:
    rollingUpdate:
      maxUnavailable: 0
  replicas: 1
  template:
    metadata:
      labels:
        app: {{ template "kuber-cli.name" . }}
        release: {{ .Release.Name }}
    spec:
      terminationGracePeriodSeconds: 0
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: kuber-role
                operator: NotIn
                values:
                - master
      containers:
        - name: kuber-cli
          image: {{ .Values.kuber-cli.image }}
          imagePullPolicy: {{ .Values.kuber-cli.imagePullPolicy }}
          volumeMounts:
          resources:
{{ toYaml .Values.kuber-cli.resources | indent 12 }} 
        - name: kuber-cli-sidecar
          image: datalayer/k8s-sidecar:1.8.2
          imagePullPolicy: IfNotPresent
          args: ["proxy", "-p", "8001"]
